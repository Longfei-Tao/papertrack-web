# PaperTrack Web 代码审查报告

> 审查日期：2026-08-19 ｜ 审查范围：functions/（后端 8 个接口 + 鉴权库）、public/（前端 4 个 JS + 页面）、schema.sql、wrangler.toml、tests/
> 审查视角：正确性 · 安全性 · 可维护性 · 性能 · 测试

---

## 一、总体评价

**整体质量很好，可以上线。** 代码风格干净、注释到位、权限模型清晰、测试 11/11 全部通过。密码哈希（PBKDF2-SHA256、10 万次迭代、随机盐）是教科书级正确；所有 SQL 均为参数化查询，无注入风险；团队数据隔离校验到位。

但在**安全**上发现 2 个需要先处理的问题（详见下文），均属于"公开部署前建议修复"的级别，修复成本很低（各 3~5 行）。

---

## 二、做得好的地方 👍

| 项目 | 说明 |
|------|------|
| 密码安全 | PBKDF2-SHA256 + 10 万次迭代 + 每用户独立随机盐；登录校验用常量时间比较，防时序攻击 |
| SQL 注入 | 全部使用 `prepare + bind` 参数化查询，无一处字符串拼接 |
| 团队隔离 | 每个接口先校验 `memberRole`，非本组成员一律 403，数据按 team_id 严格隔离 |
| 权限模型 | 普通成员只能改自己名下的论文，管理员可改全部；前后端双重校验 |
| XSS 主体防护 | 渲染用户数据时基本都经过 `esc()` 转义 |
| 错误兜底 | 前端 `unhandledrejection` 全局兜底、`file://` 打开提示、网络错误可读化 |
| 无障碍 | 弹窗焦点管理（Esc 关闭 / Tab 焦点循环 / 关闭后归还焦点）做得专业 |
| 测试 | 11 个端到端用例全部通过（注册/登录/增删改/邀请码/权限） |

---

## 三、必须修复的问题 🔴

### 🔴 1. 存储型 XSS：状态 / 分区徽章的 class 属性未转义

**位置：** `public/app.js` 的 `badge()`（450 行）、`qBadge()`（456 行）、`renderUsage()`（832 行）

**问题：** 徽章样式类名由用户可控的值直接拼进 HTML，未转义：

```js
// badge(): cls = "badge s-" + status（只删了空格，没删引号）
return `<span class="${cls}">${esc(status || "—")}${enHtml}</span>`;
```

而后端 `functions/api/papers.js` 对 `status` / `quartile` **没有做枚举校验**，任意字符串直接入库。

**攻击路径：** 组内任何成员用工具直接调接口，把某篇论文的 `status` 提交为：

```
" onmouseover="alert(document.cookie)
```

其他成员一打开看板，鼠标划过即触发脚本 —— 可窃取登录 token、冒充他人。

**修复建议（两处都做，成本极低）：**
1. 后端：`papers.js` 校验 `status` 必须属于 `STATUSES` 白名单、`quartile` 属于 `Q1~Q4/待定`，非法值直接 400。
2. 前端：`badge()` / `qBadge()` / `renderUsage()` 中的 class 改为白名单映射（或套一层 `esc()`）。

### 🔴 2. 邀请码接口权限过宽：普通成员可看到"管理员邀请码"

**位置：** `functions/api/teams/[id]/invites.js` 的 `onRequestGet`（22 行，注释写着"成员可读"）

**问题：** 任何成员（含普通成员）调用 `GET /api/teams/:id/invites` 都能拿到本组**全部**邀请码，包括 `role=admin` 的管理员邀请码。前端只是隐藏了入口，接口本身没有拦。

**攻击路径：** 普通成员拿到 admin 邀请码 → 注册新账号加入 → 自动成为管理员 → 可删改所有人论文、移除成员。

**修复建议：** `GET` 邀请码改为**仅 admin 可读**（普通成员不需要看邀请码）；或在返回前过滤掉 `role='admin'` 的码。

---

## 四、建议改进 🟡

### 🟡 3. 论文列表的 N+1 查询
`papers.js` 的 GET（95~103 行）对每篇论文单独查询一次 `paper_status_log`。论文几十篇时无感；若别人拿去塞几百篇，会明显变慢。建议改为一次 `IN (...)` 查询或 JOIN，前端按 paper_id 分组。

### 🟡 4. 登录 / 注册无速率限制
公开部署后，任何人都可对登录接口暴力试密码、或批量注册垃圾课题组。建议加 Cloudflare WAF 规则，或对 `/api/login`、`/api/register` 做简单 IP 限流（学术内部使用可接受，开源公开后值得做）。

### 🟡 5. PUT 接口对请求体的"二次读取"模式脆弱
`papers.js` 70 行和 160 行都可能消费请求体，174 行再读一次 body。当前前端总是把 `id` 放在 URL 参数里，不会触发问题；但一旦有人改用 body 传 `id`，第二次 `request.json()` 会读到空对象，`String(undefined)` 会把标题**静默改成 "undefined"**（数据破坏）。建议 PUT/DELETE 统一只从 URL 参数取 `id`。

---

## 五、小问题 💭

| 位置 | 说明 |
|------|------|
| `register.js` | 先插用户、再建团队，非事务；若第二步失败会留下"孤儿用户"（用户名被占无法重注册） |
| `register.js` / `invites.js` | 邀请码生成用 `bytes[j] % 32`，有轻微模偏差，安全上可接受 |
| `auth.js` | sessions 过期行不清理，仅查询时忽略；数据量小可接受 |
| `wrangler.toml` | `database_id` 已是真实值且已进 git 历史（该 ID 需 Cloudflare 账号权限才能操作，风险低）；README 中"替换 REPLACE_WITH_YOUR_D1_ID"的描述已与现状不符 |
| 本地预览 | `preview-server.cjs` 用明文密码，仅本地演示用（README 已注明），不是漏洞 |

---

## 六、上传前检查清单（GitHub + Cloudflare）

- [x] git 仓库已存在：`https://github.com/Longfei-Tao/papertrack-web.git`（main 分支）
- [x] `.gitignore` 已存在（233 字节），项目零依赖、无 node_modules，无多余文件
- [x] 测试 11/11 通过
- [x] 本地预览正常（http://localhost:4173）
- [x] 未提交改动：品牌化改造（brand.js / brand.default.js / brand.xmu.js / brand-logo.svg）+ 论文接口改造（first_author / 时间线等）
- [ ] 待用户确认：两个新增的 md 报告文件（`UI设计规范审查报告.md`、`开源品牌化改造报告.md`、本报告）是否随代码一起提交
- [ ] 待用户确认：是否先修复 🔴 1、🔴 2 再上传
- [ ] Cloudflare：wrangler.toml 已含真实 D1 database_id，需确认 `wrangler` 已登录、`papertrack` D1 已建表（schema.sql）

---

*报告结束。整体判断：代码质量优秀，修复 2 个安全问题后即可放心开源公开部署。*
