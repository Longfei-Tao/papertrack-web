// ============================================================
//  品牌配置入口（开源时改这里即可换机构）
// ------------------------------------------------------------
//  默认使用「学术通用主题」（不绑定任何学校，适合直接分发）。
//
//  想换成厦门大学等内置预设，把下面这行改成：
//      window.BRAND = window.BRAND_XMU;
//
//  想用自己的机构：复制 brand.xmu.js 改成 brand.myschool.js，
//  在 index.html 的 <script> 里引入它，然后在此赋值：
//      window.BRAND = window.BRAND_MYSCHOOL;
// ============================================================
window.BRAND = window.BRAND_DEFAULT;
