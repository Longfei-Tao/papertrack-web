# 开源品牌化改造报告

> 项目：课题组论文投稿进度看板（papertrack-web）
> 日期：2026-08-19
> 目标：可开源给其他团队使用 —— 不再绑定厦门大学，品牌可一键切换；同时完成视觉减法和无障碍硬伤修复。

---

## 一、核心结论

你的判断是对的：**开源版不应该只叫"厦门大学"**。这次做成了**品牌可配置架构**：

- **默认 = 中性学术主题**（墨蓝 + 通用学术徽，不绑定任何学校），拿到手就能直接用；
- **厦大 = 内置预设**（嘉庚红 + 校徽 + 嘉庚建筑 + 凤凰花 + 1921），想切回只改一行；
- **其他团队 = 复制一份配置改字段**，就能换成自己的机构。

---

## 二、怎么换品牌（对使用方来说只有一步）

编辑 `public/brand.js`：

```js
window.BRAND = window.BRAND_DEFAULT;   // 默认：中性学术主题
window.BRAND = window.BRAND_XMU;       // 改成这行：厦门大学嘉庚红主题
```

想要自己的机构：复制 `brand.xmu.js` 为 `brand.myschool.js`，改里面 5 项即可：

```js
window.BRAND_MYSCHOOL = {
  title: "XX大学 · 课题组投稿进度看板",   // 浏览器标题
  orgName: "XX大学",                      // 顶栏 / 弹窗里的机构名
  orgNameEn: "XX UNIVERSITY",             // 英文标准字
  colors: { primary: "#123456", ... },    // 主题色（主色/深/亮/浅底 4 个）
  motto: "你的校训或标语",
  assets: { logo: "./my-logo.svg", ... }, // 各 SVG 资源，null = 不用该装饰
};
```

然后在 `index.html` 里引入它（在 `brand.js` 之前），并把 `brand.js` 里那行改成 `window.BRAND = window.BRAND_MYSCHOOL;`。

---

## 三、本次改动清单

### 1. 品牌架构（新增 4 个文件）
| 文件 | 作用 |
|---|---|
| `public/brand.default.js` | 中性默认主题（墨蓝 `#2b4a6f`、通用徽标、无校色装饰） |
| `public/brand.xmu.js` | 厦大预设（嘉庚红 `#9d2235`、全部原有视觉资产） |
| `public/brand.js` | 入口：默认指向中性主题，注释说明如何切换 |
| `public/brand-logo.svg` | 中性圆形徽（展开书本 + 星，无年份，任何学校都能用） |

> 说明：厦大徽上的「1921」已直接画在 `logo-xmu.svg` 内部，配置里不需要再写年份。

### 2. 去硬编码（index.html + app.js + styles.css）
- 标题、顶栏副标题、页脚题字、弹窗 banner 的机构名/英文名、logo —— 全部改为**挂载点**，由 `applyBrand()` 在启动时注入；
- 背景图、顶栏建筑、弹窗横幅、统计卡角落装饰 —— 改为 CSS 变量 `--header-art / --modal-banner / --stat-bg / --bg-pattern`，品牌里设为 `null` 就自动退回纯色；
- 引导向导（首次弹出的使用说明）的插图改为**按品牌解析**：厦大预设显示校徽/建筑/凤凰花，中性主题统一显示中性徽；
- 清理命名：`.xmu-footer` → `.site-footer`，中性页面不再出现任何厦大字样/素材（验证：默认主题零 xmu 引用）。

### 3. 视觉减法（同时解决"太丑"）
- 中性主题**默认关闭** 6 处大装饰（全屏背景、顶栏建筑剪影、弹窗横幅、统计卡角落图、页脚建筑、凤凰花），内容区干净、更有学术气质；
- 表格状态标签的双语小字**默认隐藏**（鼠标悬停才显示英文），不再"吵"；
- 中屏下统计卡从 5 列改 2×2，不再拥挤；
- 主题色收敛：4 个品牌色 + 少量状态色，全部走 CSS 变量。

### 4. 无障碍硬伤修复（上次审查 🔴+🟡 全落地）
| 问题 | 修复 |
|---|---|
| 🔴 弹窗键盘/读屏"死" | 新增焦点管理栈：打开记录触发元素并移焦入弹窗、**Esc 关闭**、**Tab 在弹窗内循环**、关闭后焦点归还 |
| 🟡 下拉无 Esc | 全局 Esc 关闭所有下拉 |
| 🟡 Toast 读屏读不到 | 加 `role="status" aria-live="polite"` |
| 🟡 下拉按钮缺语义 | 筛选按钮与用户菜单按钮动态 `aria-expanded` + `aria-controls` |
| 🟡 输入框无标签 | 邀请码次数输入框加 `aria-label` |
| 🟡 无焦点环 | 全局 `:focus-visible` 焦点环（深色顶栏内自动反白） |

---

## 四、验证结果

- 语法检查：app.js / brand.js / brand.default.js / brand.xmu.js 全部通过；
- 资源加载：核心页面与素材全部 HTTP 200，无 404；
- 品牌解析：默认 = 中性（无任何厦大引用）；切换预设 = 厦大嘉庚红，逻辑正确；
- **自动化测试 11/11 通过**，含完整 UI 流程（注册 → 添加/编辑/删除论文 → 课题组管理 → 退出），覆盖了本次新增的弹窗焦点管理代码。

> 顺带修复了测试脚本本身两处过期问题：jsdom 需手动按序加载品牌脚本；表单 `f_impact_factor` 字段已删除，改用 `f_quartile`。

---

## 五、接下来可选

1. **上线**（当前全部为本地改动）：确认后推 main 触发 Cloudflare 部署 + 跑 D1 迁移；
2. **README 品牌化章节**：给开源仓库写"如何换品牌"的说明（也可以由我补一份，方便别人上手）；
3. 中性主题的**自定义机构示例**：如果你希望 README 里带一个完整的三方示例配置，我可以再加一份 `brand.example.js`。
